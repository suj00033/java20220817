1. Scanner 을 쓰려면 import java.util.Scanner;를 삽입해야한다.(단축키: ctrl + shift + o)

2. nextLine() : 엔터를 치기 전까지 쓴 문자열을 모두 리턴
   next() : 스페이스(공백) 전까지 입력받은 문자열을 리턴

3. Scanner scanner = new Scanner(System.in);
   사용자의 입력을 받아서 콘솔창에서 출력

4. do-while 구문
   코드블럭을 먼저 실행하고 조건문을 넣는다.

5. Method
   call method(invoke method) 함수 호출

6. ctrl + 1 : 변수이름 한번에 수정

https://limdotori.tistory.com/111